# Experimental

Esta carpeta es un laboratorio de pruebas del portal.

- La versión estable del proyecto se mantiene fuera de `experimental/`.
- Aquí puedes probar nuevas funcionalidades y ajustes antes de integrarlos a la versión principal.
- Base incluida: `index.html`, `power-bi.html`, `styles.css` y `app.js`.
